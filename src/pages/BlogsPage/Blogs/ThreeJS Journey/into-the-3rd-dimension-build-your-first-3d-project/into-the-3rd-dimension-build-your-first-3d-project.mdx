---
blogNo: 1
---

import BronoSimonVideo from "./utils/assets/bruno-simon.mp4";
import CoastalWorldVideo from "./utils/assets/coastal-world.mp4";
import LusionVideo from "./utils/assets/lusion.mp4";
import ActiveTheoryVideo from "./utils/assets/active-theory.mp4";
import withLightImage from "./utils/assets/with-lights.webp";
import noLightImage from "./utils/assets/no-lights.webp";
import { cssSandpack, htmlSandpack, jsSandpack } from "./utils/codeProviders";

export const videosData = {
  "bruno-simon": { url: "https://bruno-simon.com/", video: BronoSimonVideo },
  lusion: { url: "https://lusion.co/", video: LusionVideo },
  "active-theory": { url: "https://activetheory.net/", video: ActiveTheoryVideo },
  "coastal-world": { url: "https://coastalworld.com/", video: CoastalWorldVideo },
};

export const filesStep2 = {
  "/index.js": {
    code: `/**\n* Imports\n*/\nimport './styles.css';\nimport * as THREE from 'three';\n// console.log(THREE);\n`,
  },
  "/index.html": { code: htmlSandpack },
  "/styles.css": { code: cssSandpack },
};

export const filesFirstRender = {
  "/index.js": { code: jsSandpack },
  "/index.html": { code: htmlSandpack },
  "/styles.css": { code: cssSandpack },
};

<Intro>
Have you ever looked at a website and thought, "How on earth did they make this
magic happen? 😲" Well, I can relate to that, I was constantly amazed by some
mesmerising websites built by creative web dev studios out there. And let's be
real, a visit to
[awwwards.com](http://awwwards.com/) for me was always like stepping into a candy
store of inspiration. In my free time, I'd linger there, watching the pixelated
wonders and dreaming of creating my own masterpiece.
</Intro>

<Intro>
Then, one fine day in December 2022, I stumbled upon [Bruno
Simon's](https://bruno-simon.com/) mind-bending portfolio. It wasn't your normal
website where you scroll and browse; it was an immersive playground of colours,
physics, and sheer interactivity. I don't know whether it was the physics, the
vibrant colours, the seamless movements, or the joy of driving a virtual car
through a website, whatever it was, I knew one thing for sure—I had to learn how
to create such a wonder. And so began my journey into the world of
[Three.js,](https://threejs.org/) Fast forward to today, and here I am, having
dived headfirst into the world of 3D web development. What started as a quest to
unravel the mysteries behind Bruno Simon's portfolio turned into a delightful
journey of learning, experimenting, and making my slice of 3D wonder. And guess
what? I've decided to share the incredible things I've learned along the way with
you all. Buckle up, my fellow developers, because I'm about to take you on a ride
through the mesmerizing world of 3D web development. Let's turn your curiosity
into creation and build something truly awesome together!
</Intro>

### Why this series, and who's writing it?

Before we dive in, a quick word on why this whole series exists. When I started
learning Three.js and WebGL, I struggled a lot at first. Don't get me wrong –
the [Three.js community](https://discourse.threejs.org/) is fantastic, but even
with their support, I still found myself wandering through endless internet
rabbit holes, searching for wisdom to piece together the intricate puzzle of 3D
development.

Picture this: you've got a brilliant idea for a website, you want to create a
mesmerising, interactive landing page. You think, "let's add mind-blowing fluid
simulations", you're excited, but suddenly you're like, "Umm, but how?" That's
exactly where I found myself with my project,
[Trinity 2024](https://trinity-2024.vercel.app). After digging through countless
blog posts, I stumbled upon a gem of wisdom that lit up my path. It was like
finding the missing puzzle piece.

<InfoNote>
No doubt I will be writing a blog on this topic but till then if you want to
check out which blog post I referred to while building fluid simulation for
Trinity 2024 head over to this
[blog](https://mofu-dev.com/en/blog/stable-fluids/) post.
</InfoNote>

I built the website eventually, but it took me a lot of time. And that got me
thinking - why keep all this hard-earned knowledge to myself when I could share
it with all of you? As a software developer, I believe that once you know the
<span>why and what of something</span>, the <span>how</span> should never be an
obstacle standing in the way of doing it. And that's why I'm writing this series.

Quick intro: I'm Sahil, a <span>creative developer</span>. I've been exploring
web development for a few years, built some [awesome projects](/projects) along
the way, and I also work on <span>Full-Stack</span> apps — Python-Flask, node.js,
MongoDB, AI, ML, OpenCV, Compiler Design and much more. Feel free to discuss any
of these with me.

Over the course of this series I plan to cover, one blog post at a time:

<ul>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Getting Started with Three.js:</b> setting up your dev environment, the core concepts, and getting comfortable with its workflow.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Creating 3D Scenes:</b> modeling, texturing, lighting, and scene composition from scratch.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Animating Your Creations:</b> from simple movements to complex interactions.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Materials and Shaders:</b> adding depth, realism, and stunning visual effects to your scenes.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Post-Processing:</b> techniques to enhance visual fidelity and add cinematic effects.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Realistic Renders:</b> the techniques and best practices behind photorealistic output.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>FBO, Particles, GPU Intensive Stuff:</b> Frame Buffer Objects and GPU-intensive particle systems.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Optimizing Performance:</b> fine-tuning your code and rendering for smooth, high-performance experiences.</li>
</ul>

<MustRead>
While FBO, Shaders and Post processing are some of the fascinating topics, this
blog will not focus on them yet. These are rather advanced topics and each
deserves its own blog post.

👉 But your curious mind can further read more about them here:

<ul>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://barradeau.com/blog/?p=621" target="_blank" rel="noopener noreferrer">FBO particles</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://thebookofshaders.com/" target="_blank" rel="noopener noreferrer">The Book of Shaders</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://tympanus.net/codrops/2022/11/29/sketchy-pencil-effect-with-three-js-post-processing/" target="_blank" rel="noopener noreferrer">Post-Processing</a></li>
</ul>

😉 Don't worry, I will be writing blogs on these topics in the future. So stay
tuned!
</MustRead>

And here's the cherry on top – this entire series is absolutely free. If you ever
find yourself stuck on a tricky concept, head over to
[GitHub discussions](https://github.com/SahilK-027/sahilk027.blog/discussions) to
connect with fellow learners, ask burning questions, and share your insights.

### Meet Three.js: Your Coolest Sidekick

Now that we've set the stage for our 3D journey, let's take a closer look at our
trusty sidekick—Three.JS!

<ul>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Three.js in a Nutshell:</b> Three.js is a JavaScript library that speaks the language of browsers, allowing you to build awesome animated 3D experiences in a snap. Ever heard of <a target="_blank" rel="noopener noreferrer" href="https://en.wikipedia.org/wiki/WebGL">WebGL</a>? (Don't worry if you haven't 🤓) It's like the secret sauce behind the scenes, and Three.js uses it to make your browser a stage for dazzling 3D performances.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>No High-End PCs Required:</b> Forget about needing a supercomputer for breathtaking 3D visuals. Three.js lets you create jaw-dropping scenes with nothing but your everyday devices. Grab your smartphone, open a web browser, and voila! You're now the proud owner of a pocket-sized 3D theatre. Who needs a console when you've got Three.js in your corner?</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Three.js Ecosystem:</b> Three.JS is an <a href="https://github.com/mrdoob/three.js" target="_blank" rel="noopener noreferrer">open-source project</a> hosted on GitHub where web developers alike gather to build and maintain this awesome library.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Craft Anything, Anywhere:</b> Whether you're building a game, directing a virtual music video, visualizing scientific data, or want to build an interactive web experience, Three.js is your versatile paintbrush for the digital canvas. No need for a special device. If you have a functioning laptop and a browser, you're all set to start your creativity wherever and whenever you want.</li>
</ul>

### Some of my Favorite Three.js Projects

<VideoGrid videos={videosData} />

<InfoNote>
<p>Just four? I know, it's like trying to pick just one flavor of ice cream at an all-you-can-eat buffet - impossible! But hey, I've managed to narrow the list down to these four gems amidst the vast sea of creativity out there. But wait, don't worry! I've got a long list of other jaw-dropping projects that deserve their moment in the spotlight. It's like having a playlist with endless hits, but for web development. So, to keep this blog on track and avoid going down the rabbit hole, I've limited myself to just these four. But hey, I promise to spill the beans on all the other mind-blowing projects I've stumbled upon.</p>
<p>👉 Here is a list of some mesmerizing web experiences to watch out for:</p>
<ul>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://midwam.com/en" target="_blank" rel="noopener noreferrer">Midwam</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://www.oculus.com/medal-of-honor/" target="_blank" rel="noopener noreferrer">Medal of Honor</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://heraclosgame.com/" target="_blank" rel="noopener noreferrer">Heraclos Game</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://bambukstudio.com/" target="_blank" rel="noopener noreferrer">Bambuk Studio</a></li>
  <li><i className="fa-solid fa-arrow-right"></i> <a href="https://www.knguru.de/en" target="_blank" rel="noopener noreferrer">Knguru</a></li>
</ul>
<p>😉 Take some time and make sure that you visit them all!</p>
</InfoNote>

### The ABCs of Three.js

Alright, let's dig into the fundamental building blocks of Three.js.

<InfoNote>
In this section, I will be writing some basic lines of code no need to code them
along the way they are just there so that you will get familiar. Otherwise, we
will start building our 3D playground in the next section.
</InfoNote>

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b> Scene - The Stage for Your 3D World:</b> In the world of Three.js, a 'Scene' is the collection of all the objects, the stage where you gather your 3D stuff to interact; it's where you'll place your objects, lights, and cameras.</li>
</ul>

```js
// How to set up the scene?
const scene = new THREE.Scene();
```

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b> Camera - Your virtual DSLR: </b> The 'Camera' determines what part of the 3D world is visible to the user. The camera is like eyes through which your audience will spectate the 3D world.</li>
</ul>

```js
// How to set up the camera?
const camera = new THREE.PerspectiveCamera(
  75,
  window.innerWidth / window.innerHeight,
  0.1,
  1000
);
```

<Insight>
What are all those numbers? Let's break it down:

<ul>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Field of View (75): </b> The field of view is the extent of the observable world that is seen at any given moment. It's specified in degrees along the vertical axis. A higher value results in a wider field of view, providing a more expansive view of the 3D scene.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Aspect Ratio (window.innerWidth / window.innerHeight): </b> The aspect ratio is the ratio of the camera's width to its height. It is typically set to the width of the browser window divided by the height, ensuring that the scene appears undistorted based on the dimensions of the viewing area.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Near Clipping Plane (0.1): </b> The near clipping plane represents the closest distance from the camera at which objects are visible.</li>
  <li><i className="fa-solid fa-arrow-right"></i> <b>Far Clipping Plane (1000): </b> The far clipping plane is the farthest distance at which objects are visible. Objects beyond this distance won't be rendered.</li>
</ul>
</Insight>

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b> Renderer - The Assistant: </b> The 'Renderer' transforms your 3D scene into pixels visible on the screen.</li>
</ul>

```js
// How to set up the renderer?
const renderer = new THREE.WebGLRenderer();
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);
```

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b>Geometry - The Building Blocks: </b> 'Geometry' is where you define the shape and structure of your 3D objects. This is like the blueprint for your 3D objects. There are many 3D as well as 2D geometries that Three.js provides like cones, torus, cubes, circles, and hearts but I will let you discover those. Head over to this link to get to know more about Three.js geometries.</li>
</ul>

```js
// How to create the geometry?
const geometry = new THREE.BoxGeometry();
```

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b>Material - The Skin of Your Objects: </b> 'Material' is the outer layer of your 3D objects, defining their appearance. Like if you have a sphere and you want it to look like our globe then you can add that particular Material to your geometry. Materials are not something crazy stuff they are just JPG or PNG image files or can also be colors that you wanna define, that's it!</li>
</ul>

```js
// How to create a material?
const material = new THREE.MeshBasicMaterial({ color: 0x00ff00 });
```

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b>Mesh - Bringing Geometry and Material Together: </b> I define 'Mesh' as the marriage of geometry and material 😂, creating a tangible 3D object. It creates the complete renderable object. If Geometry == Skeleton && Material == Flesh and skin then Mesh == Complete Body</li>
</ul>

```js
// How to create a mesh?
// Combine geometry and material to create a mesh
const cube = new THREE.Mesh(geometry, material);
scene.add(cube);
```

<ul>
  <li><i className="fa-regular fa-circle-dot"></i> <b>Lights & shadows - Illuminating the Stage: </b> Before explaining you about Lights and shadows let me tell you a story of the invisible 3D model! So in 2023, I was building my first 3D website <a href="https://trinity-2023.vercel.app/" target="_blank" rel="noopener noreferrer">Trinity-2023</a>, all excited. The canvas and background both were black. I imported the 3D model of a fighter drone (Trinity bot as we call it) and added it to the scene. But wait, where is it? I was confused, what did I do wrong? I was left scratching my head for hours, changing the blender export setting, deleting the imported file, and re-importing it. But nothing was actually happening. Hours passed by, and I was trying to figure out the mystery. Why is my model invisible? Then, I read a post on the internet. Someone had dropped a simple truth bomb – "Don't forget the lights." Lights? Really? I add lights to the scene, and boom! There it is – my 3D model, finally visible. I can almost hear a cheer from my computer. Lesson learned: Lights are like the sunshine for 3D stuff. Without them, your creations are just hiding in the shadows. So, note to self and fellow coders: When things go invisible, turn on the lights! 🌟😅 Lights and shadows add realism and depth to your 3D scene If it's not already dark.</li>
</ul>

<ImageSlider leftImage={withLightImage} rightImage={noLightImage} caption="Comparison of Scene with lights and shadows and Scene without lights and shadows." />

```js
// How to add lights?
const light = new THREE.DirectionalLight(0xffffff, 1);
light.position.set(1, 1, 1).normalize();
scene.add(light);
```

### Setting Up the Project

Alright, no more chit-chat theory, let's get started with setting up your first
3D playground! Initialize the project by creating the vite app. Vite is a build
tool that aims to provide a faster and leaner development server for modern web
projects. Run the following commands in your CLI:

<h4 className="sub-title"><span>Step 1:</span> Get the dependencies</h4>

```bash
npm create vite@latest
cd project-name
npm install three
npm run dev
```

<h4 className="sub-title"><span>Step 2:</span> Import Three.JS to the project</h4>

<CodeSandpack files={filesStep2} layout="preview" />

After console logging the the THREE variable you can notice it contains most of
the classes and properties you might need on a Three.js project. Unfortunately,
not all classes are inside this variable, but we will see later how to access
them.

<InfoNote>
<p>Don't worry, for some time we won't be seeing anything until we add all the required things to build our scene, so just make sure to follow along! And as the HTML-css files will remain the same I will only add JS snippets below</p>
<p>Make sure to write all the code one after the other in given sequence only in your main.js file.</p>
</InfoNote>

<h4 className="sub-title"><span>Step 3:</span> Grab the canvas from the HTML doc</h4>

```js
/**
* Imports
*/
import './styles.css';
import * as THREE from 'three';
// console.log(THREE);

/**
* Canvas
*/
const canvas = document.querySelector("canvas.webgl");
```

<h4 className="sub-title"><span>Step 4:</span> Define your 3D scene</h4>

You place your objects, models, particles, lights, etc. in it, and at some point,
you ask Three.js to render that scene. To create a scene, use the
[Scene](https://threejs.org/docs/index.html#api/en/scenes/Scene) class:

```js
// ... Previous code

/*
* Scene
*/
const scene = new THREE.Scene();
```

<h4 className="sub-title"><span>Step 5:</span> Mesh = Geometry + Material</h4>

To create that cool purple sphere, we need to create a geometry and a material
and then combine them to create a mesh. There are many geometries and many
materials, but we will keep things simple for now and create a
[SphereGeometry](https://threejs.org/docs/index.html?q=sphere#api/en/geometries/SphereGeometry)
and a
[MeshPhongMaterial](https://threejs.org/docs/index.html?q=MeshPhongMaterial#api/en/materials/MeshPhongMaterial).

```js
// ... Previous code

/*
* Create Your Mesh
*/
// 3 parameters that correspond to the sphere's size
// radius, width segments, height segments :
const geometry = new THREE.SphereGeometry(0.8, 32, 16);

// A material for shiny surfaces with specular highlights.
//  All we need is to specify its color property and wireframe mode
const material = new THREE.MeshPhongMaterial({
  color: "#7444ff",
  wireframe: true,
});
const mesh = new THREE.Mesh(geometry, material);

// You can now add your mesh to the scene by using the add(...) method:
scene.add(mesh);
```

<h4 className="sub-title"><span>Step 6:</span> Let's add lights</h4>

```js
// ... Previous code

/*
* Lights
*/
const ambientLight = new THREE.AmbientLight(0x404040); // soft white light
const directionalLight = new THREE.DirectionalLight(0xffffff, 1);
const hemisphereLight = new THREE.HemisphereLight(0x7444ff, 0xff00bb, 0.5);
const pointLight = new THREE.PointLight(0x7444ff, 1, 100);
pointLight.position.set(0, 3, 4);

scene.add(ambientLight);
scene.add(directionalLight);
scene.add(hemisphereLight);
scene.add(pointLight);
```

<h4 className="sub-title"><span>Step 7:</span> Camera</h4>

The camera is not visible. It's more like a theoretical point of view. When we
will do a render of your scene, it will be from that camera's point of view. You
can have multiple cameras just like on a movie set, and you can switch between
those cameras as you please. Usually, we only use one camera. There are different
types of cameras, and we will talk about these in a future lesson. For now, we
simply need a camera that handles perspective (making close objects look more
prominent than far objects).

```js
// ... Previous code

/*
* Camera
*/
const sizes = {
  width: window.innerWidth,
  height: window.innerHeight
};
// PerspectiveCamera(aFOV, AspectRtio)
//
const camera = new THREE.PerspectiveCamera(75, sizes.width / sizes.height);
camera.position.z = 3;
scene.add(camera);
```

<h4 className="sub-title"><span>Step 8:</span> Renderer</h4>

The renderer's job is to do the render. Bet you didn't see that coming! We will
simply ask the renderer to render our scene from the camera's point of view, and
the result will be drawn into a canvas. You can create the canvas by yourself, or
let the renderer generate it and then add it to your page. For this exercise, we
will add the canvas to the HTML and send it to the renderer.

```js
// ... Previous code

/*
* Renderer
*/
const renderer = new THREE.WebGLRenderer({
  canvas: canvas,
  alpha: true
});
renderer.setSize(sizes.width, sizes.height);
```

<h4 className="sub-title"><span>Step 9:</span> Update the renderer on each frame</h4>

In real-time 3D applications, it's crucial to continuously render the scene to
provide smooth visual updates. This loop ensures that the scene is redrawn at the
appropriate frame rate, typically 60 frames per second (FPS) for smooth
animations. By updating the position, rotation, and other properties of objects
in the scene gradually over time (based on deltaTime), animations appear smooth
and natural to the viewer's eye. Without this time-based approach, animations
might appear choppy or inconsistent, especially on devices with varying frame
rates.

```js
// ... Previous code

/*
* Update the renderer on each frame
*/
const clock = new THREE.Clock();
let previousTime = 0;
const tick = () => {
  const elapsedTime = clock.getElapsedTime();
  const deltaTime = elapsedTime - previousTime;
  previousTime = elapsedTime;

  // Animate the mesh
  mesh.rotation.x += deltaTime * 0.7;
  mesh.rotation.y += deltaTime * 0.7;
  mesh.rotation.z += deltaTime * 0.7;

  renderer.render(scene, camera);

  // Recursively call the tick function
  window.requestAnimationFrame(tick);
};
tick();
```

Here's your first render!

<CodeSandpack files={filesFirstRender} layout="preview" />

### Conclusion

Congratulations on taking your first steps into the mesmerizing world of 3D web
development with Three.js! Throughout this article, you've delved into the
fundamentals of creating immersive 3D scenes, learned to use the power of lights,
materials, geometries, and cameras, and even added interactive controls to your
scenes. As you continue your exploration of Three.js and 3D web development,
remember that practice, curiosity, and persistence are keys. Embrace the
challenges, celebrate your successes, and don't hesitate to dive deeper into the
vast resources available, including documentation, tutorials, and online
communities. So, fellow developer, let your curiosity guide you, your creativity
fuel you, and your passion for learning push you forward. Happy coding 🚀!
